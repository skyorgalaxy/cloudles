# 408 数据结构：栈知识点补充

> 来源定位：`408pdf/2027王道《数据结构》考研复习指导...pdf`  
> 你说的“66 页（PDF 77）”主要是：`GetTop`、共享栈、链栈。  
> “67 页（PDF 78）”开始是本节试题精选，核心考点不是背定义，而是会判断栈顶指针、出栈序列、链栈操作。

---

## 1. 先把栈看成一句话

栈是**只允许在一端插入和删除的线性表**。

- 插入：入栈 `Push`
- 删除：出栈 `Pop`
- 读取栈顶：`GetTop`
- 操作端：栈顶
- 规则：后进先出 `LIFO`

最容易混的地方：

> 栈的逻辑结构还是线性结构。  
> “受限”限制的是操作位置，不是说它变成非线性结构。

所以：

- 栈和队列的共同点：都是线性结构
- 栈和队列的不同点：运算规则不同
- 栈不允许直接删除栈底元素，这不是基本操作

---

## 2. 顺序栈：题目真正考的是 top 的含义

顺序栈就是用数组存栈。

做题时不要死背 `++top` 或 `top++`，先问自己一句：

> `top` 指向栈顶元素，还是指向栈顶元素的下一个位置？

### 2.1 top 指向栈顶元素

常见设定：

```c
top = -1; // 空栈
```

此时：

```c
Push: data[++top] = x;
Pop : x = data[top--];
GetTop: x = data[top];
```

判空：

```c
top == -1
```

若数组下标是 `0 ~ n-1`，判满：

```c
top == n - 1
```

记忆方式：

> 空栈时没有栈顶元素，所以 `top = -1`。  
> 入栈要先把 top 移到新位置，再放元素。

### 2.2 top 指向栈顶元素的下一个位置

常见设定：

```c
top = 0; // 空栈
```

此时：

```c
Push: data[top++] = x;
Pop : x = data[--top];
GetTop: x = data[top - 1];
```

判空：

```c
top == 0
```

若数组下标是 `0 ~ n-1`，判满：

```c
top == n
```

记忆方式：

> `top` 像“下一个空位”的指针。  
> 入栈先占当前空位，再往后挪。

### 2.3 栈向低地址增长

有些题会说：

```c
data[1...n], top = n + 1
```

这表示栈从高地址往低地址长，`top` 初始在数组右边界外。

入栈：

```c
data[--top] = x;
```

出栈：

```c
x = data[top++];
```

这类题不要被 `n+1` 吓到，核心还是：

> 先判断栈往哪边长，再判断 top 是不是已经指向栈顶。

---

## 3. GetTop 和 Pop 的区别

`GetTop`：只读栈顶，不改变栈。

```c
bool GetTop(SqStack S, ElemType &x) {
    if (S.top == -1) return false;
    x = S.data[S.top];
    return true;
}
```

`Pop`：删除栈顶，并返回栈顶元素。

```c
bool Pop(SqStack &S, ElemType &x) {
    if (S.top == -1) return false;
    x = S.data[S.top--];
    return true;
}
```

题 12 这种就考这里：

```c
Push(a);
Push(b);
Pop(x);     // x = b，b 出栈
GetTop(x);  // x = a，a 还在栈里
```

最终 `x = a`。

---

## 4. 共享栈：两个栈从数组两端向中间长

共享栈适合两个栈共用一段数组。

设数组为：

```c
Share[0...n-1]
```

两个栈：

```c
top1 = -1; // 左栈为空
top2 = n;  // 右栈为空
```

左栈向右增长：

```c
Share[++top1] = x;
```

右栈向左增长：

```c
Share[--top2] = x;
```

栈满条件：

```c
top2 - top1 == 1
```

也就是：

> 两个栈顶相邻，中间没有空位。

共享栈的好处：

- 节省存储空间
- 降低上溢可能
- 不会降低单次存取效率，入栈/出栈仍是 `O(1)`

注意：

> 共享栈解决的是“空间利用率”和“上溢风险”，不是解决下溢。

---

## 5. 链栈：一般在表头入栈、表头出栈

链栈通常用单链表实现。

王道这一节默认：

> 不带头结点，`top` 直接指向栈顶元素。

### 5.1 不带头结点链栈

入栈，把新结点插到表头：

```c
x->next = top;
top = x;
```

出栈：

```c
x = top->data;
top = top->next;
```

注意：严格写代码时还要保存旧结点并释放：

```c
p = top;
x = p->data;
top = p->next;
free(p);
```

### 5.2 链栈相比顺序栈的优势

链栈可以动态申请结点，所以通常不会出现“栈满”。

但它不是不会栈空。空栈照样会发生。

---

## 6. 出栈序列题：最重要的 4 个判断法

67 页后面很多题都是“给定入栈序列，判断某个出栈序列是否可能”。

### 方法 1：模拟法，最稳

给定入栈序列 `1 2 3 4`，判断出栈序列 `1 3 4 2`。

规则：

1. 想要输出某个元素，若它不在栈顶，就继续入栈。
2. 若目标元素在栈顶，就出栈。
3. 若目标元素已经入过栈，但不在栈顶，就不可能。

例：

```text
目标输出：1 3 4 2

入 1，出 1
入 2
入 3，出 3
入 4，出 4
出 2
```

所以可行。

### 方法 2：第一个出栈元素决定栈内逆序

若入栈序列是：

```text
1 2 3 4 5
```

如果第一个出栈的是 `4`，说明此时：

```text
1 2 3 4
```

都已经入栈，`4` 出栈后，栈里从顶到底是：

```text
3 2 1
```

所以如果接下来没有新元素干扰，`1 2 3` 的相对出栈顺序必须是逆序的一部分。

常用判错句：

> 某元素出栈时，比它早入栈且还没出栈的元素，之后只能按逆序出栈。

例如 `a b c d` 入栈，若 `d` 第一个出栈，则后面只能是：

```text
d c b a
```

所以 `d c a b` 不可能。

### 方法 3：入栈出栈操作串合法性

用 `I` 表示入栈，`O` 表示出栈。

合法条件：

1. 任意前缀中，`O` 的个数不能超过 `I`。
2. 最终 `I` 的总数要等于 `O` 的总数。

例如：

```text
IOO
```

第二个操作后已经空栈，再 `O` 就非法。

也可以理解成：

```text
I = +1
O = -1
```

任意前缀和不能小于 0，最终和必须等于 0。

### 方法 4：出栈序列个数用 Catalan 数

`n` 个不同元素按固定顺序入栈，可能的出栈序列个数是：

```text
C_n = 1 / (n + 1) * C(2n, n)
```

小值记一下：

| n | 出栈序列个数 |
|---|---:|
| 1 | 1 |
| 2 | 2 |
| 3 | 5 |
| 4 | 14 |
| 5 | 42 |

题 13 问 3 个元素，就是 `5` 种。

---

## 7. 做选择题的速查表

对应 PDF 第 78-84 页，本节选择题 01-31。

| 题号 | 答案 | 主要考点 |
|---:|:---:|---|
| 01 | B | 栈和队列都是线性结构 |
| 02 | C | 栈是限制存取点的线性结构 |
| 03 | B | 删除栈底不是基本操作 |
| 04 | C | `top=-1`，入栈 `a[++top]=x` |
| 05 | B | `top` 指向下一个位置，入栈 `data[top++]=x` |
| 06 | A | 向低地址增长，入栈 `data[--top]=x` |
| 07 | A | 栈顶指针随 Push/Pop 加减 |
| 08 | A | 链栈通常不栈满 |
| 09 | C | 头指针单向循环链表不适合表头操作后找尾 |
| 10 | C | 不带头结点链栈入栈 |
| 11 | D | 不带头结点链栈出栈 |
| 12 | A | `Pop` 改栈，`GetTop` 不改栈 |
| 13 | B | 3 个元素出栈序列数为 5 |
| 14 | D | 出栈序列合法性 |
| 15 | A | 以 `cd` 开头只剩 `ba` |
| 16 | D | 按目标序列模拟 S/X |
| 17 | D | 第一个出栈为 n，则整体逆序 |
| 18 | D | 第一个出栈为 i，后续不唯一 |
| 19 | C | 判断不可能的出栈序列 |
| 20 | C | 局部逆序约束 |
| 21 | A | 局部模拟 |
| 22 | C | 注意最后入栈元素的位置 |
| 23 | C | 栈序列 + C 标识符 |
| 24 | B | 共享栈节省空间、降低上溢 |
| 25 | A | 共享栈满：`top2 - top1 == 1` |
| 26 | C | 模拟栈深度最大值 |
| 27 | D | 不允许连续 3 次出栈 |
| 28 | B | 以 d 开头的序列计数 |
| 29 | C | `P1=3` 时 `P2` 可取除 3 外所有数 |
| 30 | D | 操作序列模拟 |
| 31 | D | 入栈序列和出栈序列可相同，也可互为倒序 |

---

## 8. 课后题卡住时，按这个流程做

### 第一步：先标注 top 含义

看到顺序栈题，先写：

```text
top 指向：栈顶元素 / 栈顶下一个位置
增长方向：高地址 / 低地址
初值：-1 / 0 / 1 / n+1
```

再决定用：

```text
++top
top++
--top
top--
```

### 第二步：出栈序列题直接模拟

画两列：

```text
待入栈：1 2 3 4
栈：
目标输出：
```

每次只做两件事：

- 目标在栈顶：出栈
- 目标不在栈顶：继续入栈

如果目标已经被压在下面，却要求它先出栈，就是不可能。

### 第三步：遇到“个数”题，优先穷举小规模

408 选择题里，`n` 一般不大。

例如 4 个元素以 `cd` 开头：

```text
a 入
b 入
c 入，c 出
d 入，d 出
```

此时栈里只剩：

```text
b
a
```

所以只能：

```text
c d b a
```

答案就是 1 个。

---

## 9. 本节最容易错的句子

- 栈是线性结构，不是非线性结构。
- 栈不一定是顺序存储，也可以链式存储。
- `GetTop` 不会删除元素。
- 顺序栈的入栈写法取决于 `top` 的定义。
- 链栈通常在表头操作。
- 不带头结点链栈入栈是 `x->next=top; top=x;`。
- 共享栈满不是 `top1 == top2`，而是两个栈顶相邻。
- 出栈序列题不要凭感觉，模拟一定能做。

---

## 10. 给自己的一句提醒

这节题看起来散，其实只有两条主线：

> 一条是“栈顶指针到底指哪儿”。  
> 一条是“先进后出导致的相对顺序约束”。

做题时先抓这两条，其他都是表面变化。
